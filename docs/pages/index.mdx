import { Tab, Tabs } from 'nextra-theme-docs'
import { Hero } from '../components/Hero'
import {
  Container,
  ExampleList,
  ExampleWrapper,
  ArrayExample,
  BasicExample,
  SerializerExample,
  DropInstanceExample,
} from '../components/Interactive'

<Hero />

<Container>

## Installation

Install the module with your favorite package manager:

<Tabs items={["npm", "pnpm", "yarn"]}>
  <Tab>

```shell filename="Terminal"
npm install react-forage
```

  </Tab>
  <Tab>

```shell filename="Terminal"
pnpm add react-forage
```

  </Tab>
  <Tab>

```shell filename="Terminal"
yarn add react-forage
```

  </Tab>
</Tabs>

## Examples

In the following example, state data will be stored in IndexedDB. You can switch to the Application panel in the browser console (press `F12`). In IndexedDB, you can see a database named `react-forage-website` and a data table named `examples`.

<ExampleList>

### Store state into IndexedDB

<ExampleWrapper>
  Refresh this page and you will get the state from IndexedDB.
  <BasicExample />
</ExampleWrapper>

### Store complex types of data

<ExampleWrapper>
  The `useLocalForageState` hook will do the serialization and deserialization
  work automatically.
  <ArrayExample />
</ExampleWrapper>

### Custom serialization and deserialization functions

<ExampleWrapper>
  You may not need the default `JSON.stringify` or `JSON.parse` to serialize
  string.
  <SerializerExample />
</ExampleWrapper>

### Drops store of the current instance

<ExampleWrapper>
  Please open the console to view the database changes of IndexedDB. When
  invoked with no arguments, it drops the “store” of the current instance.
  <DropInstanceExample />
</ExampleWrapper>

</ExampleList>

</Container>
